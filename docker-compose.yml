version: '3.8'

services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: myPortfolio
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
      - PORT=12300
    networks:
      - proxy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:12300"]
      interval: 5m
      timeout: 10s
      retries: 3
      start_period: 10s

networks:
  proxy:
    external: true